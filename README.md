# Bilds Russia RP Website
Your supplied logo is already included as `assets/logo.png`.

Open `index.html` to preview it.
For GitHub Pages, upload ALL files and folders, then use:
Settings → Pages → Deploy from branch → main → /(root).

Replace `https://discord.gg/YOURINVITE` in `index.html` with your real Discord invite.
The displayed player count is a demo until a real server API is connected.
